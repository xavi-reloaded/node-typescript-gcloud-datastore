# node-typescript-gcloud-datastore

A Node/TypeScript seed project to manage Google Cloud Datastore Buckets .

## Docker

````
docker build . -t json-server-datastorage
````

````
sudo docker run -d -p 9090:9090 json-server-datastorage
````